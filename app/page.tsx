import * as data from './data';
import { Header, Footer, JsonLd } from './components';
import { fleetServices } from './fleet-content';
const d=data as any;
const site=d.site||{};
const posts=(d.blogPosts||[]).slice(0,3);
const stats=(d.stats||[]).slice(0,3);
const providers=(d.providerCards||[]).slice(0,3);
const featuredRoles=fleetServices.slice(0,6);
const shortlistChecks=[
  {number:'01',title:'Candidate proof',body:'Ask for a paid work sample that matches the real job. Use safe sample data and score the result before sharing live accounts.'},
  {number:'02',title:'Named manager',body:'Get the name of the person who checks attendance, answers questions, and helps when the work misses the mark.'},
  {number:'03',title:'Access plan',body:'Start each Filipino assistant with their own account and only the tools needed for the first task. Add access after clean work.'},
  {number:'04',title:'Replacement terms',body:'Ask what starts a replacement, how long a new Philippines-based match may take, and who protects the handoff.'},
];
const title=(x:any)=>typeof x==='string'?x:(x.title||x.name||x.label||x.question||'Assistant role');
const text=(x:any)=>typeof x==='string'?x:(x.desc||x.excerpt||x.note||x.body||(x.bestFor?`Best for ${x.bestFor.join(', ')}`:'Clear tasks, safe access, and review rules.'));
const domain=site.domain||site.brand||'Staffing Guide';
const heroImage=site.heroImage||site.serviceImage||'https://images.unsplash.com/photo-1551836022-d5d88e9218df?auto=format&fit=crop&w=1200&q=80';
export default function Home(){const schema={'@context':'https://schema.org','@type':'WebSite',name:site.brand,url:`https://${domain}`};return <><Header/><main className="belay"><JsonLd data={schema}/>
<section className="hero"><div className="container hero-grid"><div className="copy"><p className="eyebrow">Philippines virtual assistant staffing</p><h1>Hire Filipino talent with a clear role and a safer start.</h1><p className="lead">Tell us what work needs doing. We help shape the role, match it with Philippines-based talent, and plan the first handoff.</p><div className="actions"><a className="btn primary" href="/contact">Request a Philippines staffing plan</a><a className="btn secondary" href="#tasks">Browse Filipino VA roles</a></div><p className="risk">Rates depend on the role, hours, and skills. Every staffing path on this site is for talent based in the Philippines.</p></div><div className="match-card"><div className="portrait-wrap"><img src={heroImage} alt={site.alt||'Virtual assistant planning a managed client handoff'}/><span className="badge">Philippines-only matching</span></div><div className="task-note note-a"><b>Daily handoff</b><span>an owner sets priorities</span></div><div className="task-note note-b"><b>Work review</b><span>check output each week</span></div><div className="task-note note-c"><b>First few weeks</b><span>scope, shadow, then live work</span></div></div></div><div className="container proof-bar"><span>Plan the role before filling it</span>{stats.length?stats.map((s:any,i:number)=><b key={i}>{s.label}: {s.value}</b>):['Scope first','7-21 days','5-10 tasks'].map((x,i)=><b key={i}>{x}</b>)}</div></section>
<section className="role-board" id="tasks"><div className="container"><div className="role-board-head"><div><p className="eyebrow">Philippines role desk</p><h2>Choose the work lane before you compare candidates.</h2><p>Each guide shows tasks, access limits, and a simple launch plan for a Filipino specialist. Start with one lane. You can add more once the first work is steady.</p><a className="text-link dark-link" href="/services">View all Philippines-based services</a></div><figure><img src="https://images.unsplash.com/photo-1521737711867-e3b97375f902?auto=format&fit=crop&w=1200&q=82" alt="Business support team discussing virtual assistant role plans in an office"/><figcaption>Write down the work, tools, hours, and approval rules before interviews begin.</figcaption></figure></div><div className="role-card-grid">{featuredRoles.map((role,i)=><a className="role-card" key={role.slug} href={`/services/${role.slug}`}><span>{String(i+1).padStart(2,'0')}</span><h3>{role.title}</h3><p>{role.summary}</p><b>See tasks and controls</b></a>)}</div></div></section>
<section className="provider-compare" id="compare"><div className="container"><div className="compare-intro"><div><p className="eyebrow">Compare Philippines hiring paths</p><h2>Pick the support model that fits the work.</h2><p>Each path below is for Filipino talent. A managed provider can help with screening and backup. A specialist team may know your tools. A direct Filipino freelancer gives you a closer working relationship, but you handle the hiring checks.</p><div className="compare-actions"><a className="btn primary" href="/contact">Get a Philippines role plan</a><a className="text-link" href="/blog/virtual-assistant-vs-employee">Compare a Filipino VA with an employee</a></div></div><figure className="compare-image"><img src="https://images.unsplash.com/photo-1552581234-26160f608093?auto=format&fit=crop&w=1200&q=82" alt="Business owners comparing Philippines virtual assistant hiring options at a meeting"/><figcaption>Compare ownership, backup, and quality checks before comparing rates.</figcaption></figure></div><div className="provider-grid">{providers.map((provider:any,i:number)=><article className="provider-card" key={provider.name}><div className="provider-number">0{i+1}</div><p className="provider-fit">{provider.rating}</p><h3>{provider.name}</h3><p>{provider.note}</p><ul><li>Who screens the Filipino assistant?</li><li>Who checks the work?</li><li>What happens if the fit fails?</li></ul></article>)}</div></div></section>
<section className="provider-check"><div className="container"><div className="provider-check-head"><div><p className="eyebrow">Provider shortlist scorecard</p><h2>Score the provider before you compare the price.</h2><p>A polished sales call tells you very little about day-to-day support. Ask how the company tests Filipino candidates, checks the work, limits access, and handles a failed fit. Write down each answer so every provider gets the same test.</p><div className="provider-check-actions"><a className="btn primary" href="/contact">Build my Philippines role brief</a><a className="text-link dark-link" href="/research/hire-virtual-assistant-philippines-evidence-guide">Read the Philippines hiring evidence guide</a></div></div><figure><img src="https://images.unsplash.com/photo-1454165804606-c3d57bc86b40?auto=format&fit=crop&w=1200&q=82" alt="Business team reviewing a provider shortlist and project notes at a table"/><figcaption>Use one scorecard for every provider. Notes beat memory after the third sales call.</figcaption></figure></div><div className="provider-check-grid">{shortlistChecks.map(check=><article key={check.number}><span>{check.number}</span><h3>{check.title}</h3><p>{check.body}</p></article>)}</div></div></section>
<section className="container section guide-row"><div><p className="eyebrow">Before you hire</p><h2>Read the short buyer guides.</h2></div>{posts.map((p:any,i:number)=><a href={`/blog/${p.slug}`} key={i}><span>{p.minutes||7} min</span><strong>{title(p)}</strong><p>{text(p)}</p></a>)}</section>
<section className="container final"><h2>Know what your Filipino assistant will own before the interviews start.</h2><a className="btn primary" href="/contact">Request a Philippines staffing plan</a></section>
</main><Footer/></>}
